const BLC = require(__dirname + '/../blc');
  const slc = require(__dirname + '/../../slc');

  module.exports = class KousaBLC extends BLC {
    process(input){
      return new Promise((resolve, reject) => {
      });
    }
  };